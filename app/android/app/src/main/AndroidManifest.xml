<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- O app é um cliente do Portal: sem rede ele não tem o que mostrar. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--
      Os avisos do petshop (etapa 9 — push). Do Android 13 em diante a notificação pede
      permissão como a câmera pede, e sem esta linha o pedido nem aparece: o sistema
      responde "negado" calado.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="Meu PetShop AI"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
          O aviso que chega com o app fechado é desenhado pelo Firebase, não pelo app, e
          ele precisa saber qual ícone pôr na barra de status. O ícone da barra é uma
          silhueta branca — o colorido do lançador vira um quadrado cinza ali —, e a pata
          chapada do ícone temático é exatamente isso.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_launcher_mono" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="avisos" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!--
          O recibo do Financeiro abre no navegador do aparelho, por uma URL assinada
          (`abrirEndereco`, em `lib/src/arquivos.dart`). Do Android 11 em diante um app
          **não enxerga** os outros sem declarar o que pretende abrir: sem esta entrada,
          o `url_launcher` não acha quem atenda https e o toque em "Baixar recibo" não
          faz nada — sem erro, sem log e sem nada a investigar.
        -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
